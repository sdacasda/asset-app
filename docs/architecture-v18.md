# v18 mobile pagination polish

This version improves the mobile bottom pagination UI.

Changes:

- Mobile pagination uses a compact two-row layout.
- Page jump input is clearer and easier to tap.
- Previous/next buttons are disabled at boundaries.
- One-page result sets automatically collapse pagination controls.
- No backend data model changes.
