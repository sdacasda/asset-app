# v16 批量同步进度反馈

v16 针对“同步全部启用”只显示顶部提示、不显示当前任务进度的问题进行了修复。

## 主要调整

- 前端维护 `currentTaskIds`，用于追踪批量同步创建的多个任务。
- `/api/sync_tasks/all_enabled` 返回的 task_id 会被加入轮询列表。
- 当前任务面板展示批量同步总体进度，包括完成数、成功数、异常数和每个任务的单独进度条。
- 取消任务支持批量取消。

## 保留能力

- v15 的旧网址别名归类和页码跳转保留。
- v14/v13 的同步任务持久化和 PC 端表格优化保留。
